services:
  - type: web
    name: qunex-trade
    env: python
    buildCommand: bash build.sh
    startCommand: cd web && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
